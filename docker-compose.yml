version: '3.8'

services:
  claw-agent:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    ports:
      - "8080:8080"
    environment:
      - PYTHONUNBUFFERED=1